<!DOCTYPE html>
<html lang="pt-BR" class="h-full" data-theme="light">
<head>
    <meta charset="UTF-8">
    <title>Dashboard – <%= (typeof org !== 'undefined') ? org.name : 'AgendaPro' %></title>    
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
        (function() {
            const THEMES = ["light", "medium-dark", "deep-dark"];
            const savedTheme = localStorage.getItem('theme');
            let initialTheme = 'light';

            if (savedTheme && THEMES.includes(savedTheme)) {
                initialTheme = savedTheme;
            } else if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
                initialTheme = 'deep-dark';
            }
            document.documentElement.setAttribute('data-theme', initialTheme);
        })();
    </script>

    <style>
        /* ================================================= */
        /* === 1. DEFINIÇÃO DAS CORES PARA TRÊS TEMAS === */
        /* ================================================= */

        /* --- TEMA 1: LIGHT --- */
        :root, [data-theme="light"] {
            --color-gold-light: #FFD700;
            --color-gold-dark: #B8860B;
            --color-gold-accent: #DAA520;
            --color-bg-primary: #f3f4f6;   /* Fundo Geral */
            --color-bg-secondary: #ffffff; /* Fundo Cards */
            --color-bg-muted: #f9fafb;     
            --color-bg-accent-light: #FFFBEB;
            --color-bg-accent-dark: #fefce8;
            --color-text-primary: #1f2937; /* Texto Escuro */
            --color-text-secondary: #4b5563;
            --color-text-accent: #B8860B;
            --color-text-accent-muted: #b45309;
            --color-border: #e5e7eb;
        }

        /* --- TEMA 2: MEDIUM DARK --- */
        [data-theme="medium-dark"] {
            --color-gold-light: #FFD700;
            --color-gold-dark: #e0aa1f;
            --color-gold-accent: #DAA520;
            --color-bg-primary: #1f2937;   /* Cinza Escuro */
            --color-bg-secondary: #374151; /* Cinza Médio */
            --color-bg-muted: #4b5563;     
            --color-bg-accent-light: #422c01;
            --color-bg-accent-dark: #423001;
            --color-text-primary: #f9fafb; /* Texto Claro */
            --color-text-secondary: #d1d5db;
            --color-text-accent: #e0aa1f;
            --color-text-accent-muted: #fde047;
            --color-border: #4b5563;
        }

        /* --- TEMA 3: DEEP DARK --- */
        [data-theme="deep-dark"] {
            --color-gold-light: #FFD700;
            --color-gold-dark: #e0aa1f;
            --color-gold-accent: #DAA520;
            --color-bg-primary: #0a0e14;   /* Quase Preto */
            --color-bg-secondary: #1f2937; /* Cinza Escuro */
            --color-bg-muted: #374151;
            --color-bg-accent-light: #422c01;
            --color-bg-accent-dark: #423001;
            --color-text-primary: #f9fafb;
            --color-text-secondary: #d1d5db;
            --color-text-accent: #e0aa1f;
            --color-text-accent-muted: #fde047;
            --color-border: #4b5563;
        }

        /* ================================================= */
        /* === 2. ESTILOS BASE === */
        /* ================================================= */
        
        body { 
            height: 100%; 
            overflow: hidden; 
            display: flex; 
            flex-direction: column; 
            background-color: var(--color-bg-primary); 
            color: var(--color-text-primary);
        } 
        
        /* Mapeamento */
        .bg-white, .bg-gray-800 { background-color: var(--color-bg-secondary); }
        .bg-gray-100, .bg-gray-900 { background-color: var(--color-bg-primary); }
        .bg-gray-50, .bg-gray-700\/50, .bg-gray-700\/30 { background-color: var(--color-bg-muted); }
        .bg-gold-light { background-color: var(--color-bg-accent-light); }
        
        .border-gray-200, .border-gray-700 { border-color: var(--color-border); }
        .border-gray-300, .border-gray-600 { border-color: var(--color-border); }
        
        .text-gray-900, .text-gray-800 { color: var(--color-text-primary); }
        .text-gray-600, .text-gray-700 { color: var(--color-text-secondary); }
        .text-gray-500 { color: var(--color-text-secondary); }
        
        /* --- CORREÇÃO MENU MOBILE --- */
        .mobile-nav-link { 
            color: var(--color-text-primary);
            transition: color 0.2s ease;
        }
        .mobile-nav-link:hover {
            color: var(--color-text-accent);
        }

        /* Utilitários */
        .time-badge {
            background-color: var(--color-bg-accent-dark);
            color: var(--color-text-accent-muted);
            padding: 0.25rem 0.75rem;
            border-radius: 9999px;
            font-size: 0.75rem;
            font-weight: 600;
        }

        /* Scrollbar */
        .custom-scroll::-webkit-scrollbar { width: 6px; }
        .custom-scroll::-webkit-scrollbar-track { background: transparent; }
        .custom-scroll::-webkit-scrollbar-thumb { background-color: var(--color-border); border-radius: 20px; }
        .custom-scroll:hover::-webkit-scrollbar-thumb { background-color: var(--color-gold-accent); }
        
        .text-gold { color: var(--color-text-accent); }
        .bg-gold { background-color: var(--color-gold-accent); }
        .border-gold { border-color: var(--color-gold-light); }
        
        .text-gradient-gold {
            background: linear-gradient(90deg, #B8860B, #FFD700, #DAA520, #FFD700, #B8860B);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            background-size: 200% auto;
            animation: text-shine 4s linear infinite;
        }
        @keyframes text-shine { from { background-position: 200% center; } to { background-position: 0% center; } }
        
        .bg-shine-gold { position: relative; overflow: hidden; }
        .bg-shine-gold::after {
            content: ''; position: absolute; top: 0; left: -100%; width: 50%; height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            animation: bg-shine 4s infinite linear;
        }
        @keyframes bg-shine { 0% { left: -100%; } 100% { left: 200%; } }
        
        .appointment-card { transition: all 0.2s ease; border-left: 3px solid transparent; }
        .appointment-card:hover { border-left-color: var(--color-gold-dark); background-color: var(--color-bg-muted); }

        .compact-card { display: flex; flex-direction: column; height: 100%; }
        .compact-header { flex-shrink: 0; }
        .compact-body { flex-grow: 1; overflow-y: auto; }
    </style>
</head>
<body class="flex flex-col h-full">
    
    <% if (locals.originalAdminSession) { %>
      <div class="bg-yellow-400 text-yellow-900 text-center text-xs py-1 font-bold z-[60]">
        <i class="fas fa-user-secret"></i> Personificando Organização
        <a href="/master/stop-impersonation" class="underline ml-2">Sair</a>
      </div>
    <% } %>

    <header class="bg-white shadow-sm border-b border-gray-200 shrink-0 h-16 flex items-center z-50">
        <div class="w-full max-w-full px-4 flex items-center justify-between">
            <div class="flex items-center shrink-0 mr-4">
                <h1 class="flex items-center text-gray-800 text-lg font-bold">
                   Agenda Pro
                </h1>
                <div class="h-6 w-px bg-gray-200 hidden sm:block ml-4"></div>
            </div>

            <nav class="hidden xl:flex items-center space-x-4 text-sm whitespace-nowrap overflow-x-auto custom-scroll pb-1 lg:pb-0">
                <!-- <a href="/dashboard" class="text-gradient-gold font-semibold">
                    <i class="fas fa-home mr-1"></i>Dash
                </a> -->
                </a> <a href="/admin/whatsapp" class="text-gray-600 hover:text-gold transition-colors">
                    <i class="fas fa-calendar-alt mr-1"></i>Conectar whatsapp🟩💬
                </a>
                <a href="/agendamentos-por-dia" class="text-gray-600 hover:text-gold transition-colors">
                    <i class="fas fa-calendar-alt mr-1"></i>Agenda
                </a>
                <a href="/clients" class="text-gray-600 hover:text-gold transition-colors">
                    <i class="fas fa-users mr-1"></i>Clientes
                </a>
                <a href="/admin/servicos" class="text-gray-600 hover:text-gold transition-colors">
                    <i class="fas fa-cut mr-1"></i>Serviços
                </a>
                <a href="/admin/equipe" class="text-gray-600 hover:text-gold transition-colors">
                    <i class="fas fa-users-cog mr-1"></i>Equipe
                </a>
                <a href="/financeiro" class="text-gray-600 hover:text-gold transition-colors">
                    <i class="fas fa-dollar-sign mr-1"></i>Financeiro
                </a>
                <a href="/balanco" class="text-gray-600 hover:text-gold transition-colors">
                    <i class="fas fa-balance-scale mr-1"></i>Balanço
                </a>
                <a href="/expenses" class="text-gray-600 hover:text-gold transition-colors">
                    <i class="fas fa-receipt mr-1"></i>Despesas
                </a>
                
                <div class="h-4 w-px bg-gray-300 mx-2"></div>

                <button id="theme-toggle-btn" class="p-2 rounded-lg hover:bg-gray-100 transition-colors text-gray-600">
                    <i id="theme-icon-sun" class="fas fa-sun"></i> <i id="theme-icon-moon" class="fas fa-moon hidden"></i> 
                </button>
                
                <a href="/logout" class="text-red-600 hover:text-red-700 transition-colors font-semibold">
                    <i class="fas fa-sign-out-alt mr-1"></i>Sair
                </a>
            </nav>

            <button id="menu-button" class="xl:hidden p-2 rounded-lg hover:bg-gray-100 transition-colors">
                <i class="fas fa-bars text-gold"></i>
            </button>
        </div>
    </header>

    <div id="mobile-menu" class="hidden fixed inset-0 z-50 flex flex-col p-6 space-y-4 overflow-y-auto xl:hidden" style="background-color: var(--color-bg-primary);">
        <div class="flex justify-end">
            <button id="close-menu" class="text-2xl" style="color: var(--color-text-primary);">
                <i class="fas fa-times"></i>
            </button>
        </div>
        <div class="flex flex-col space-y-3 text-center">
            <a href="/admin/whatsapp" class="mobile-nav-link text-lg font-bold py-2 border-b border-gray-700" style="color: var(--color-text-accent);">Conectar whatsapp🟩💬</a>
            <a href="/agendamentos-por-dia" class="mobile-nav-link text-lg py-2">Agendamentos</a>
            <a href="/clients" class="mobile-nav-link text-lg py-2">Clientes</a>
            <a href="/admin/servicos" class="mobile-nav-link text-lg py-2">Serviços</a>
            <a href="/admin/equipe" class="mobile-nav-link text-lg py-2">Equipe</a>
            <a href="/financeiro" class="mobile-nav-link text-lg py-2">Financeiro</a>
            <a href="/balanco" class="mobile-nav-link text-lg py-2">Balanço</a>
            <a href="/expenses" class="mobile-nav-link text-lg py-2">Despesas</a>
            
            <button id="theme-toggle-btn-mobile" class="w-full flex items-center justify-center space-x-3 p-3 rounded-lg mobile-nav-link hover:bg-gray-700 hover:text-gold border border-gray-500 mt-4">
                <i id="theme-icon-sun-mobile" class="fas fa-sun w-5 text-center"></i>
                <i id="theme-icon-moon-mobile" class="fas fa-moon w-5 text-center hidden"></i>
                <span>Alternar Tema</span>
            </button>

            <a href="/logout" class="text-red-500 text-lg py-2 font-bold mt-4">Sair do Sistema</a>
        </div>
    </div>

    <main class="flex-1 p-3 overflow-hidden flex flex-col gap-3 max-w-full mx-auto w-full">
        
            <div class="grid grid-cols-1 md:grid-cols-4 gap-3 shrink-0">
                <div class="col-span-1 md:col-span-4 bg-gold bg-shine-gold rounded-lg p-3 text-white flex flex-col justify-center shadow-sm">
                    <div class="flex justify-between items-start">
                        <div>
                            <h2 class="text-md font-bold leading-tight">Olá, <%= (typeof org !== 'undefined') ? org.name : 'Admin' %>!</h2>
                            <p class="text-xs opacity-90 mt-1"><%= new Date().toLocaleDateString('pt-BR', { weekday: 'long', day: 'numeric', month: 'short' }) %></p>
                        </div>
                        <i class="fas fa-crown opacity-30 text-2xl"></i>
                    </div>
                </div>
            </div>
            <div class="col-span-1 md:col-span-4 bg-white rounded-lg p-3 border border-gray-200 shadow-sm flex flex-col justify-center">
                <div class="flex items-center justify-between mb-1">
                    <span class="text-xs font-bold text-gray-500 uppercase">
                        <i class="fas fa-link mr-1"></i> Link de Agendamento
                    </span>
                    <a href="/salao/<%= org.slug %>" target="_blank" class="text-xs text-blue-500 hover:underline">
                        Abrir <i class="fas fa-external-link-alt ml-1"></i>
                    </a>
                </div>
                
                <div class="flex items-center bg-gray-50 rounded border border-gray-200 p-2">
                    <input type="text" readonly 
                           value="<%= process.env.BASE_URL || 'http://localhost:3003' %>/salao/<%= org.slug %>" 
                           class="bg-transparent text-sm text-gray-600 w-full outline-none truncate mr-2"
                           id="publicLinkInput">
                    
                    <button onclick="copyLink()" class="text-gold hover:text-gold-dark transition-colors" title="Copiar Link">
                        <i class="far fa-copy text-lg"></i>
                    </button>
                </div>
            </div>

            <div class="bg-white rounded-lg p-3 border border-gray-200 shadow-sm flex items-center justify-between">
                <div>
                    <p class="text-xs text-gray-500 font-semibold uppercase">Receita Hoje</p>
                    <p class="text-lg font-bold text-emerald-600">R$ <%= (receitaHoje || 0).toFixed(2).replace('.', ',') %></p>
                </div>
                <div class="w-10 h-10 bg-emerald-100 rounded-full flex items-center justify-center text-emerald-600"><i class="fas fa-coins"></i></div>
            </div>

            <div class="bg-white rounded-lg p-3 border border-gray-200 shadow-sm flex items-center justify-between hidden md:flex">
                <div>
                    <p class="text-xs text-gray-500 font-semibold uppercase">Semana</p>
                    <p class="text-lg font-bold text-yellow-600">R$ <%= (receitaSemana || 0).toFixed(2).replace('.', ',') %></p>
                </div>
                <div class="w-10 h-10 bg-yellow-100 rounded-full flex items-center justify-center text-yellow-600"><i class="fas fa-chart-line"></i></div>
            </div>

            <div class="bg-white rounded-lg p-3 border border-gray-200 shadow-sm flex items-center justify-between hidden md:flex">
                <div>
                    <p class="text-xs text-gray-500 font-semibold uppercase">Mês Atual</p>
                    <p class="text-lg font-bold text-blue-600">R$ <%= (receitaMes || 0).toFixed(2).replace('.', ',') %></p>
                </div>
                <div class="w-10 h-10 bg-blue-100 rounded-full flex items-center justify-center text-blue-600"><i class="fas fa-calendar"></i></div>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-12 gap-3 flex-1 min-h-0">
            
            <div class="lg:col-span-2 flex flex-col gap-3 overflow-y-auto custom-scroll">
                <div class="bg-white rounded-lg border border-gray-200 shadow-sm p-3">
                    <h3 class="text-xs font-bold text-gray-500 uppercase mb-3 text-center">Ações</h3>
                    <div class="grid grid-cols-2 lg:grid-cols-1 gap-2">
                        <a href="/agendamentos-por-dia" class="flex flex-col items-center p-2 rounded-md bg-gray-50 hover:bg-gold-light hover:text-gold-dark transition text-xs font-medium text-center">
                            <i class="fas fa-plus-circle text-gold text-lg mb-1"></i> Novo Agend.
                        </a>
                        <a href="/clients" class="flex flex-col items-center p-2 rounded-md bg-gray-50 hover:bg-gold-light hover:text-gold-dark transition text-xs font-medium text-center">
                            <i class="fas fa-user-plus text-blue-500 text-lg mb-1"></i> Novo Cliente
                        </a>
                    </div>
                </div>

                <div class="bg-white rounded-lg border border-gray-200 shadow-sm p-3 flex-1">
                    <h3 class="text-xs font-bold text-gray-500 uppercase mb-2 text-center">Status</h3>
                    <div class="flex flex-col items-center justify-center h-full">
                        <div class="flex items-center space-x-2 text-xs text-green-600 mb-2">
                            <div class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></div>
                            <span>Sistema Online</span>
                        </div>
                         <div class="text-xs text-gray-400 text-center">Atualizado<br>Agora mesmo</div>
                    </div>
                </div>
            </div>

            <div class="lg:col-span-5 bg-white rounded-lg border border-gray-200 shadow-sm compact-card">
                <div class="compact-header p-3 border-b border-gray-100 bg-gray-50 flex justify-between items-center">
                    <h3 class="font-bold text-gray-800 text-sm flex items-center">
                        <i class="fas fa-calendar-day text-gold mr-2"></i>Hoje
                    </h3>
                    <% if (proximosHoje && proximosHoje.length > 0) { %>
                        <span class="text-xs font-bold bg-gold text-white px-2 py-0.5 rounded-full"><%= proximosHoje.length %></span>
                    <% } %>
                </div>
                
                <div class="compact-body p-2 custom-scroll space-y-2">
                    <% if (!proximosHoje || proximosHoje.length === 0) { %>
                        <div class="h-full flex flex-col items-center justify-center text-gray-400 py-10">
                            <i class="fas fa-check-circle text-3xl mb-2 opacity-50 text-green-400"></i>
                            <p class="text-sm">Agenda livre hoje!</p>
                        </div>
                    <% } else { %>
                        <% proximosHoje.forEach((item) => { %>
                            <div class="appointment-card bg-gray-50 p-2 rounded border border-gray-100 flex justify-between items-center group relative overflow-hidden">
                                <div class="w-1 bg-gold absolute left-0 top-0 bottom-0"></div>
                                <div class="flex items-center gap-3 pl-2 overflow-hidden w-full">
                                    <div class="flex flex-col items-center justify-center min-w-[50px] border-r border-gray-200 pr-2">
                                        <span class="font-bold text-gray-800 text-sm"><%= item.time %></span>
                                    </div>
                                    <div class="min-w-0 flex-1">
                                        <div class="font-semibold text-sm truncate text-gray-800"><a href="/client/<%= item.clientId %>" class="hover:text-gold"><%= item.name %></a></div>
                                        <div class="text-xs text-gray-500 truncate flex items-center">
                                            <i class="fas fa-cut text-[10px] mr-1"></i> <%= item.service %>
                                        </div>
                                    </div>
                                    <a href="/client/<%= item.clientId %>" class="text-gray-400 hover:text-gold p-2 transition-colors">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                </div>
                            </div>
                        <% }); %>
                    <% } %>
                </div>
            </div>

            <div class="lg:col-span-5 bg-white rounded-lg border border-gray-200 shadow-sm compact-card">
                <div class="compact-header p-3 border-b border-gray-100 bg-gray-50 flex justify-between items-center">
                    <h3 class="font-bold text-gray-800 text-sm flex items-center">
                        <i class="fas fa-sun text-gray-400 mr-2"></i>Amanhã
                    </h3>
                    <% if (proximosAmanha && proximosAmanha.length > 0) { %>
                        <span class="text-xs font-bold bg-gray-200 text-gray-600 px-2 py-0.5 rounded-full"><%= proximosAmanha.length %></span>
                    <% } %>
                </div>

                <div class="compact-body p-2 custom-scroll space-y-2">
                    <% if (!proximosAmanha || proximosAmanha.length === 0) { %>
                        <div class="h-full flex flex-col items-center justify-center text-gray-400 py-10">
                            <i class="far fa-calendar text-3xl mb-2 opacity-50"></i>
                            <p class="text-sm">Nada agendado.</p>
                        </div>
                    <% } else { %>
                        <% proximosAmanha.forEach((item) => { %>
                            <div class="appointment-card bg-gray-50 p-2 rounded border border-gray-100 flex justify-between items-center group opacity-80 hover:opacity-100">
                                <div class="flex items-center gap-3 pl-1 overflow-hidden w-full">
                                     <div class="flex flex-col items-center justify-center min-w-[50px] border-r border-gray-200 pr-2">
                                        <span class="font-bold text-gray-600 text-sm"><%= item.time %></span>
                                    </div>
                                    <div class="min-w-0 flex-1">
                                        <div class="font-semibold text-sm truncate text-gray-700"><a href="/client/<%= item.clientId %>"><%= item.name %></a></div>
                                        <div class="text-xs text-gray-500 truncate"><%= item.service %></div>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    <% } %>
                </div>
            </div>

        </div>
        
        <div id="dashboard-data" class="hidden"
            data-hoje='<%- JSON.stringify(proximosHoje || []) %>'
            data-amanha='<%- JSON.stringify(proximosAmanha || []) %>'
            data-pending='<%- JSON.stringify(pendingAppointments || []) %>'>
        </div>
    </main>

    <script>
        // =================================================
        // === LÓGICA JAVASCRIPT PARA CICLO DE TEMA ===
        // =================================================

        const THEMES = ["light", "medium-dark", "deep-dark"];
        const htmlEl = document.documentElement;

        function getNextTheme(currentTheme) {
            const currentIndex = THEMES.indexOf(currentTheme);
            const nextIndex = (currentIndex + 1) % THEMES.length;
            return THEMES[nextIndex];
        }

        function updateIcons(theme) {
            const sunDesktop = document.getElementById('theme-icon-sun');
            const moonDesktop = document.getElementById('theme-icon-moon');
            const sunMobile = document.getElementById('theme-icon-sun-mobile'); 
            const moonMobile = document.getElementById('theme-icon-moon-mobile'); 
            
            const isLight = theme === 'light';

            if (sunDesktop) sunDesktop.classList.toggle('hidden', !isLight);
            if (moonDesktop) moonDesktop.classList.toggle('hidden', isLight);
            
            if (sunMobile) sunMobile.classList.toggle('hidden', !isLight);
            if (moonMobile) moonMobile.classList.toggle('hidden', isLight);
        }

        function toggleTheme() {
            const currentTheme = htmlEl.getAttribute('data-theme') || 'light';
            const newTheme = getNextTheme(currentTheme);

            htmlEl.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            updateIcons(newTheme);
        }

        document.addEventListener('DOMContentLoaded', function() {
             const initialTheme = htmlEl.getAttribute('data-theme') || 'light';
             updateIcons(initialTheme);
             
             const toggleButtonDesktop = document.getElementById('theme-toggle-btn');
             const toggleButtonMobile = document.getElementById('theme-toggle-btn-mobile'); 

             if (toggleButtonDesktop) {
                 toggleButtonDesktop.addEventListener('click', toggleTheme);
             }
             if (toggleButtonMobile) { 
                 toggleButtonMobile.addEventListener('click', toggleTheme);
             }
        });
        // FIM DA LÓGICA DE TEMA ============================

        // Toggle Menu Mobile
        const menuButton = document.getElementById('menu-button');
        const mobileMenu = document.getElementById('mobile-menu');
        const closeMenu = document.getElementById('close-menu');
        if(menuButton && mobileMenu && closeMenu) {
            menuButton.addEventListener('click', () => mobileMenu.classList.remove('hidden'));
            closeMenu.addEventListener('click', () => mobileMenu.classList.add('hidden'));
        }

        // Logica de Alertas Pendentes
        document.addEventListener('DOMContentLoaded', function() {
            const dataEl = document.getElementById('dashboard-data');
            if(!dataEl) return;

            try {
                const pending = JSON.parse(dataEl.dataset.pending || '[]');
                if (pending.length > 0) {
                    let html = '<div class="text-left text-sm max-h-60 overflow-y-auto space-y-2 p-1">';
                    pending.forEach(appt => {
                        const date = new Date(appt.date).toLocaleString('pt-BR', {day:'2-digit', month:'2-digit', hour:'2-digit', minute:'2-digit'});
                        const cName = appt.Client ? appt.Client.name : 'Cliente';
                        const sName = (appt.AppointmentServices && appt.AppointmentServices[0]?.name) || (appt.AppointmentProducts && appt.AppointmentProducts[0]?.name) || 'Serviço';
                        const pId = appt.id;
                        
                        html += `
                        <div class="bg-gray-100 p-3 rounded flex flex-col gap-2 border border-gray-200">
                            <div><strong class="text-gold-dark">${cName}</strong> <span class="text-gray-500 text-xs">(${sName})</span></div>
                            <div class="text-xs text-gray-600"><i class="far fa-clock"></i> ${date}</div>
                            <div class="flex gap-2 mt-1">
                                <form action="/admin/appointment/${pId}/confirm" method="POST" class="flex-1"><button class="w-full bg-green-100 text-green-700 py-1 rounded text-xs hover:bg-green-200 font-bold">Confirmar</button></form>
                                <form action="/admin/appointment/${pId}/cancel-by-admin" method="POST" class="flex-1" onsubmit="return promptReason(this)"><button class="w-full bg-red-100 text-red-700 py-1 rounded text-xs hover:bg-red-200 font-bold">Cancelar</button></form>
                            </div>
                        </div>`;
                    });
                    html += '</div>';
                    
                    Swal.fire({
                        title: `<span class="text-lg">🔔 ${pending.length} Pendente(s)</span>`,
                        html: html,
                        showConfirmButton: false,
                        showCloseButton: true,
                        width: '400px',
                        customClass: { popup: 'bg-white dark:bg-gray-800' }
                    });
                }
            } catch(e) { console.error(e); }
        });

        function promptReason(form) {
            const r = prompt("Motivo do cancelamento:");
            if(!r || r.trim() === "") return false;
            const i = document.createElement('input'); i.type='hidden'; i.name='cancellationReason'; i.value=r; form.appendChild(i);
            return true;
        }
        function copyLink() {
        const copyText = document.getElementById("publicLinkInput");
        copyText.select();
        copyText.setSelectionRange(0, 99999); // Mobile
        navigator.clipboard.writeText(copyText.value).then(() => {
            // Feedback visual simples (pode usar SweetAlert se preferir)
            const btn = document.querySelector('button[onclick="copyLink()"] i');
            btn.className = "fas fa-check text-green-500";
            setTimeout(() => { btn.className = "far fa-copy text-lg"; }, 2000);
        });
    }
    </script>
</body>
</html>