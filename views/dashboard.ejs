<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Dashboard – Studio Kadosh</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        .gradient-purple {
            background: linear-gradient(135deg, #a855f7 0%, #ec4899 100%);
        }
        .gradient-blue {
            background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
        }
        .gradient-green {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
        }
        .gradient-yellow {
            background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
        }
        .gradient-red {
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
        }
        .gradient-indigo {
            background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
        }
        .card-hover {
            transition: all 0.3s ease;
        }
        .card-hover:hover {
            transform: translateY(-2px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }
        .glass-effect {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        .animate-fade-in {
            animation: fadeIn 0.5s ease-in;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .appointment-card {
            transition: all 0.3s ease;
            border-left: 4px solid transparent;
        }
        .appointment-card:hover {
            border-left-color: #8b5cf6;
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
        }
        .metric-card {
            background: linear-gradient(135deg, rgba(255,255,255,0.9) 0%, rgba(248,250,252,0.9) 100%);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255,255,255,0.2);
        }
        .pulse-animation {
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }
        .time-badge {
            background: linear-gradient(135deg, #e0e7ff 0%, #c7d2fe 100%);
            color: #3730a3;
            padding: 0.25rem 0.75rem;
            border-radius: 9999px;
            font-size: 0.75rem;
            font-weight: 600;
        }
    </style>
</head>
<body class="min-h-screen bg-gradient-to-br from-purple-50 via-pink-50 to-blue-50">
    <!-- HEADER -->
    <header class="glass-effect sticky top-0 z-50 border-b border-purple-100">
        <div class="max-w-7xl mx-auto px-4 py-4">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-4">
                    <h1 class="text-2xl font-bold bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                        Studio Kadosh
                    </h1>
                    <div class="h-6 w-px bg-purple-200"></div>
                    <span class="text-lg font-semibold text-gray-700 flex items-center">
                        <i class="fas fa-home mr-2 text-purple-600"></i>
                        Dashboard
                    </span>
                </div>
                
                <!-- Desktop Navigation -->
                <nav class="hidden lg:flex items-center space-x-6">
                    <a href="/dashboard" class="text-purple-600 font-semibold">
                        <i class="fas fa-home mr-2"></i>Dashboard
                    </a>
                    <a href="/agendamentos-por-dia" class="text-gray-600 hover:text-purple-600 transition-colors">
                        <i class="fas fa-calendar-alt mr-2"></i>Agendamentos
                    </a>
                    <a href="/clients" class="text-gray-600 hover:text-purple-600 transition-colors">
                        <i class="fas fa-users mr-2"></i>Clientes
                    </a>
                    <a href="/financeiro" class="text-gray-600 hover:text-purple-600 transition-colors">
                        <i class="fas fa-dollar-sign mr-2"></i>Financeiro
                    </a>
                    <a href="/balanco" class="text-gray-600 hover:text-purple-600 transition-colors">
                        <i class="fas fa-balance-scale mr-2"></i>Balanço
                    </a>
                    <a href="/expenses" class="text-gray-600 hover:text-purple-600 transition-colors">
                        <i class="fas fa-receipt mr-2"></i>Despesas
                    </a>
                    <a href="/logout" class="text-red-600 hover:text-red-700 transition-colors">
                        <i class="fas fa-sign-out-alt mr-2"></i>Sair
                    </a>
                </nav>

                <!-- Mobile Menu Button -->
                <button id="menu-button" class="lg:hidden p-2 rounded-lg hover:bg-purple-100 transition-colors">
                    <i class="fas fa-bars text-purple-600"></i>
                </button>
            </div>

            <!-- Mobile Menu -->
            <div id="mobile-menu" class="hidden lg:hidden mt-4 space-y-2">
                <a href="/dashboard" class="flex items-center space-x-3 p-3 rounded-lg bg-gradient-to-r from-gray-600 to-gray-700 text-white hover:opacity-90 transition-opacity">
                    <i class="fas fa-home"></i>
                    <span>🏠 Dashboard</span>
                </a>
                <a href="/agendamentos-por-dia" class="flex items-center space-x-3 p-3 rounded-lg gradient-blue text-white hover:opacity-90 transition-opacity">
                    <i class="fas fa-calendar-alt"></i>
                    <span>📅 Agendamentos</span>
                </a>
                <a href="/clients" class="flex items-center space-x-3 p-3 rounded-lg gradient-purple text-white hover:opacity-90 transition-opacity">
                    <i class="fas fa-users"></i>
                    <span>👥 Clientes</span>
                </a>
                <a href="/financeiro" class="flex items-center space-x-3 p-3 rounded-lg gradient-green text-white hover:opacity-90 transition-opacity">
                    <i class="fas fa-dollar-sign"></i>
                    <span>💵 Financeiro</span>
                </a>
                <a href="/balanco" class="flex items-center space-x-3 p-3 rounded-lg gradient-yellow text-white hover:opacity-90 transition-opacity">
                    <i class="fas fa-balance-scale"></i>
                    <span>⚖️ Balanço</span>
                </a>
                <a href="/expenses" class="flex items-center space-x-3 p-3 rounded-lg bg-gradient-to-r from-pink-500 to-rose-500 text-white hover:opacity-90 transition-opacity">
                    <i class="fas fa-receipt"></i>
                    <span>💸 Despesas</span>
                </a>
                <a href="/logout" class="flex items-center space-x-3 p-3 rounded-lg gradient-red text-white hover:opacity-90 transition-opacity">
                    <i class="fas fa-sign-out-alt"></i>
                    <span>⬅️ Sair</span>
                </a>
            </div>
        </div>
    </header>

    <main class="max-w-7xl mx-auto px-4 py-8 space-y-8">
        <!-- Welcome Section -->
        <section class="bg-white rounded-2xl shadow-xl overflow-hidden card-hover animate-fade-in">
            <div class="gradient-bg p-6">
                <div class="flex items-center justify-between">
                    <div>
                        <h1 class="text-2xl font-bold text-white">
                            Bem-vindo ao Studio Kadosh! 
                            <span class="wave-animation">👋</span>
                        </h1>
                        <p class="text-purple-100 mt-2">
                            <i class="fas fa-calendar mr-2"></i>
                            <%= new Date().toLocaleDateString('pt-BR', { 
                                weekday: 'long', 
                                year: 'numeric', 
                                month: 'long', 
                                day: 'numeric' 
                            }) %>
                        </p>
                    </div>
                    <div class="hidden md:block">
                        <div class="w-16 h-16 bg-white bg-opacity-20 rounded-full flex items-center justify-center pulse-animation">
                            <i class="fas fa-crown text-2xl text-white"></i>
                        </div>
                    </div>
                </div>
            </div>
        </section>


        <!-- Ações Rápidas -->
        <section class="bg-white rounded-2xl shadow-xl overflow-hidden card-hover animate-fade-in">
            <div class="bg-gradient-to-r from-gray-700 to-gray-800 p-6">
                <h3 class="text-xl font-bold text-white flex items-center">
                    <i class="fas fa-bolt mr-3"></i>
                    Ações Rápidas
                </h3>
            </div>
            <div class="p-6">
                <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                    <a href="/agendamentos-por-dia" class="flex flex-col items-center p-4 gradient-blue text-white rounded-xl hover:opacity-90 transition-opacity card-hover">
                        <i class="fas fa-calendar-plus text-2xl mb-2"></i>
                        <span class="text-sm font-semibold text-center">Novo Agendamento</span>
                    </a>
                    <a href="/clients" class="flex flex-col items-center p-4 gradient-purple text-white rounded-xl hover:opacity-90 transition-opacity card-hover">
                        <i class="fas fa-user-plus text-2xl mb-2"></i>
                        <span class="text-sm font-semibold text-center">Novo Cliente</span>
                    </a>
                    <a href="/financeiro" class="flex flex-col items-center p-4 gradient-green text-white rounded-xl hover:opacity-90 transition-opacity card-hover">
                        <i class="fas fa-dollar-sign text-2xl mb-2"></i>
                        <span class="text-sm font-semibold text-center">Financeiro</span>
                    </a>
                    <a href="/balanco" class="flex flex-col items-center p-4 gradient-yellow text-white rounded-xl hover:opacity-90 transition-opacity card-hover">
                        <i class="fas fa-chart-bar text-2xl mb-2"></i>
                        <span class="text-sm font-semibold text-center">Relatórios</span>
                    </a>
                </div>
            </div>
        </section>
       
        <!-- Próximos Agendamentos -->
        <section class="grid grid-cols-1 lg:grid-cols-2 gap-8">
            <!-- HOJE -->
            <div class="bg-white rounded-2xl shadow-xl overflow-hidden card-hover animate-fade-in">
                <div class="gradient-purple p-6">
                    <div class="flex items-center justify-between">
                        <h2 class="text-xl font-bold text-white flex items-center">
                            <i class="fas fa-clock mr-3"></i>
                            Agendamentos Hoje
                        </h2>
                        <% if (proximosHoje && proximosHoje.length > 0) { %>
                        <span class="bg-white bg-opacity-20 text-white px-3 py-1 rounded-full text-sm font-semibold">
                            <%= proximosHoje.length %>
                        </span>
                        <% } %>
                    </div>
                </div>
                <div class="p-6">
                    <% if (!proximosHoje || proximosHoje.length === 0) { %>
                    <div class="text-center py-8">
                        <div class="w-20 h-20 mx-auto mb-4 bg-purple-100 rounded-full flex items-center justify-center">
                            <i class="fas fa-calendar-check text-3xl text-purple-400"></i>
                        </div>
                        <h3 class="text-lg font-semibold text-gray-900 mb-2">Nenhum agendamento hoje</h3>
                        <p class="text-gray-600 mb-4">Aproveite para organizar ou descansar!</p>
                        <a href="/agendamentos-por-dia" class="inline-flex items-center px-4 py-2 gradient-purple text-white rounded-lg hover:opacity-90 transition-opacity">
                            <i class="fas fa-plus mr-2"></i>
                            Criar Agendamento
                        </a>
                    </div>
                    <% } else { %>
                    <div class="space-y-4">
                        <% proximosHoje.forEach((item, index) => { %>
                        <div class="appointment-card bg-gradient-to-r from-gray-50 to-purple-50 p-4 rounded-xl border border-purple-100">
                            <div class="flex items-center justify-between">
                                <div class="flex-1">
                                    <div class="flex items-center space-x-3 mb-2">
                                        <div class="w-10 h-10 gradient-purple rounded-full flex items-center justify-center">
                                            <i class="fas fa-user text-white text-sm"></i>
                                        </div>
                                        <div>
                                            <h4 class="font-semibold text-gray-900">
                                                <a href="/client/<%= item.clientId %>" class="text-purple-700 hover:text-purple-800 transition-colors">
                                                    <%= item.name %>
                                                </a>
                                            </h4>
                                            <div class="flex items-center space-x-2 text-sm text-gray-600">
                                                <span class="time-badge">
                                                    <i class="fas fa-clock mr-1"></i>
                                                    <%= item.time %>
                                                </span>
                                                <span>•</span>
                                                <span><%= item.service %></span>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="flex items-center space-x-2">
                                    <a href="/client/<%= item.clientId %>" class="p-2 text-purple-600 hover:bg-purple-100 rounded-lg transition-colors">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <div class="w-2 h-2 bg-purple-500 rounded-full pulse-animation"></div>
                                </div>
                            </div>
                        </div>
                        <% }); %>
                    </div>
                    <div class="mt-6 text-center">
                        <a href="/agendamentos-por-dia" class="inline-flex items-center px-4 py-2 bg-purple-100 text-purple-700 rounded-lg hover:bg-purple-200 transition-colors">
                            <i class="fas fa-calendar-alt mr-2"></i>
                            Ver Todos os Agendamentos
                        </a>
                    </div>
                    <% } %>
                </div>
            </div>

            <!-- AMANHÃ -->
            <div class="bg-white rounded-2xl shadow-xl overflow-hidden card-hover animate-fade-in">
                <div class="gradient-indigo p-6">
                    <div class="flex items-center justify-between">
                        <h2 class="text-xl font-bold text-white flex items-center">
                            <i class="fas fa-calendar-plus mr-3"></i>
                            Agendamentos Amanhã
                        </h2>
                        <% if (proximosAmanha && proximosAmanha.length > 0) { %>
                        <span class="bg-white bg-opacity-20 text-white px-3 py-1 rounded-full text-sm font-semibold">
                            <%= proximosAmanha.length %>
                        </span>
                        <% } %>
                    </div>
                </div>
                <div class="p-6">
                    <% if (!proximosAmanha || proximosAmanha.length === 0) { %>
                    <div class="text-center py-8">
                        <div class="w-20 h-20 mx-auto mb-4 bg-indigo-100 rounded-full flex items-center justify-center">
                            <i class="fas fa-calendar-times text-3xl text-indigo-400"></i>
                        </div>
                        <h3 class="text-lg font-semibold text-gray-900 mb-2">Nenhum agendamento amanhã</h3>
                        <p class="text-gray-600 mb-4">Que tal agendar alguns clientes?</p>
                        <a href="/agendamentos-por-dia" class="inline-flex items-center px-4 py-2 gradient-indigo text-white rounded-lg hover:opacity-90 transition-opacity">
                            <i class="fas fa-plus mr-2"></i>
                            Criar Agendamento
                        </a>
                    </div>
                    <% } else { %>
                    <div class="space-y-4">
                        <% proximosAmanha.forEach((item, index) => { %>
                        <div class="appointment-card bg-gradient-to-r from-gray-50 to-indigo-50 p-4 rounded-xl border border-indigo-100">
                            <div class="flex items-center justify-between">
                                <div class="flex-1">
                                    <div class="flex items-center space-x-3 mb-2">
                                        <div class="w-10 h-10 gradient-indigo rounded-full flex items-center justify-center">
                                            <i class="fas fa-user text-white text-sm"></i>
                                        </div>
                                        <div>
                                            <h4 class="font-semibold text-gray-900">
                                                <a href="/client/<%= item.clientId %>" class="text-indigo-700 hover:text-indigo-800 transition-colors">
                                                    <%= item.name %>
                                                </a>
                                            </h4>
                                            <div class="flex items-center space-x-2 text-sm text-gray-600">
                                                <span class="bg-indigo-100 text-indigo-700 px-2 py-1 rounded-full text-xs font-medium">
                                                    <i class="fas fa-clock mr-1"></i>
                                                    <%= item.time %>
                                                </span>
                                                <span>•</span>
                                                <span><%= item.service %></span>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="flex items-center space-x-2">
                                    <a href="/client/<%= item.clientId %>" class="p-2 text-indigo-600 hover:bg-indigo-100 rounded-lg transition-colors">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <div class="w-2 h-2 bg-indigo-500 rounded-full"></div>
                                </div>
                            </div>
                        </div>
                        <% }); %>
                    </div>
                    <div class="mt-6 text-center">
                        <a href="/agendamentos-por-dia" class="inline-flex items-center px-4 py-2 bg-indigo-100 text-indigo-700 rounded-lg hover:bg-indigo-200 transition-colors">
                            <i class="fas fa-calendar-alt mr-2"></i>
                            Ver Todos os Agendamentos
                        </a>
                    </div>
                    <% } %>
                </div>
            </div>
        </section>


         <!-- Receita Total -->
        <section class="bg-white rounded-2xl shadow-xl overflow-hidden card-hover animate-fade-in">
            <div class="gradient-green p-6">
                <h2 class="text-xl font-bold text-white flex items-center">
                    <i class="fas fa-chart-line mr-3"></i>
                    Receita Total
                </h2>
            </div>
            <div class="p-6">
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                    <!-- Hoje -->
                    <div class="metric-card p-6 rounded-xl border border-green-200 card-hover">
                        <div class="flex items-center justify-between mb-4">
                            <div class="w-12 h-12 gradient-green rounded-full flex items-center justify-center">
                                <i class="fas fa-calendar-day text-white"></i>
                            </div>
                            <span class="text-sm font-medium text-green-700 bg-green-100 px-3 py-1 rounded-full">
                                Hoje
                            </span>
                        </div>
                        <div class="text-3xl font-bold text-green-800 mb-2">
                            R$ <%= receitaHoje.toFixed(2) %>
                        </div>
                        <div class="text-sm text-green-600 flex items-center">
                            <i class="fas fa-clock mr-1"></i>
                            Atualizado agora
                        </div>
                    </div>

                    <!-- Semana -->
                    <div class="metric-card p-6 rounded-xl border border-yellow-200 card-hover">
                        <div class="flex items-center justify-between mb-4">
                            <div class="w-12 h-12 gradient-yellow rounded-full flex items-center justify-center">
                                <i class="fas fa-calendar-week text-white"></i>
                            </div>
                            <span class="text-sm font-medium text-yellow-700 bg-yellow-100 px-3 py-1 rounded-full">
                                Semana
                            </span>
                        </div>
                        <div class="text-3xl font-bold text-yellow-800 mb-2">
                            R$ <%= receitaSemana.toFixed(2) %>
                        </div>
                        <div class="text-sm text-yellow-600 flex items-center">
                            <i class="fas fa-trending-up mr-1"></i>
                            Últimos 7 dias
                        </div>
                    </div>

                    <!-- Mês -->
                    <div class="metric-card p-6 rounded-xl border border-blue-200 card-hover">
                        <div class="flex items-center justify-between mb-4">
                            <div class="w-12 h-12 gradient-blue rounded-full flex items-center justify-center">
                                <i class="fas fa-calendar-alt text-white"></i>
                            </div>
                            <span class="text-sm font-medium text-blue-700 bg-blue-100 px-3 py-1 rounded-full">
                                Mês
                            </span>
                        </div>
                        <div class="text-3xl font-bold text-blue-800 mb-2">
                            R$ <%= receitaMes.toFixed(2) %>
                        </div>
                        <div class="text-sm text-blue-600 flex items-center">
                            <i class="fas fa-chart-bar mr-1"></i>
                            <%= new Date().toLocaleDateString('pt-BR', { month: 'long' }) %>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        

        <!-- Status do Sistema -->
        <section class="bg-white rounded-2xl shadow-xl overflow-hidden card-hover animate-fade-in">
            <div class="bg-gradient-to-r from-green-500 to-emerald-600 p-6">
                <h3 class="text-xl font-bold text-white flex items-center">
                    <i class="fas fa-heartbeat mr-3"></i>
                    Status do Sistema
                </h3>
            </div>
            <div class="p-6">
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                    <div class="flex items-center space-x-3 p-4 bg-green-50 rounded-xl">
                        <div class="w-3 h-3 bg-green-500 rounded-full pulse-animation"></div>
                        <div>
                            <div class="font-semibold text-green-800">Sistema Online</div>
                            <div class="text-sm text-green-600">Funcionando perfeitamente</div>
                        </div>
                    </div>
                    <div class="flex items-center space-x-3 p-4 bg-blue-50 rounded-xl">
                        <div class="w-3 h-3 bg-blue-500 rounded-full"></div>
                        <div>
                            <div class="font-semibold text-blue-800">Última Atualização</div>
                            <div class="text-sm text-blue-600">Agora mesmo</div>
                        </div>
                    </div>
                    <div class="flex items-center space-x-3 p-4 bg-purple-50 rounded-xl">
                        <div class="w-3 h-3 bg-purple-500 rounded-full"></div>
                        <div>
                            <div class="font-semibold text-purple-800">Versão</div>
                            <div class="text-sm text-purple-600">Studio Kadosh v2.0</div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <script>
        // Mobile menu toggle
        const menuButton = document.getElementById('menu-button');
        const mobileMenu = document.getElementById('mobile-menu');
        
        if (menuButton && mobileMenu) {
            menuButton.addEventListener('click', () => {
                mobileMenu.classList.toggle('hidden');
            });
        }

        // Inicialização das animações
        document.addEventListener('DOMContentLoaded', function() {
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };

            const observer = new IntersectionObserver(function(entries) {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }
                });
            }, observerOptions);

            document.querySelectorAll('.animate-fade-in').forEach(el => {
                el.style.opacity = '0';
                el.style.transform = 'translateY(20px)';
                el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
                observer.observe(el);
            });

            // Atualização automática do horário
            function updateTime() {
                const now = new Date();
                const timeElements = document.querySelectorAll('.current-time');
                timeElements.forEach(el => {
                    el.textContent = now.toLocaleTimeString('pt-BR');
                });
            }
            
            setInterval(updateTime, 1000);
        });

        // Efeito de onda no emoji
        const waveAnimation = document.querySelector('.wave-animation');
        if (waveAnimation) {
            setInterval(() => {
                waveAnimation.style.transform = 'rotate(20deg)';
                setTimeout(() => {
                    waveAnimation.style.transform = 'rotate(-20deg)';
                    setTimeout(() => {
                        waveAnimation.style.transform = 'rotate(0deg)';
                    }, 150);
                }, 150);
            }, 3000);
        }
    </script>
</body>
</html>