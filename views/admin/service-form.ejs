<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title><%= pageTitle %> – AgendaPro</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --color-gold-light: #FFD700;
            --color-gold-dark: #B8860B;
            --color-gold-accent: #DAA520;
        }
        .text-gold-dark { color: var(--color-gold-dark); }
        .bg-gold { background-color: var(--color-gold-accent); }
        .border-gold { border-color: var(--color-gold-accent); }
        .ring-gold-dark { --tw-ring-color: var(--color-gold-dark); }
        .hover\:bg-gold-dark:hover { background-color: var(--color-gold-dark); }
        /* Estilos para inputs focados */
        .form-input:focus, .form-textarea:focus, .form-checkbox:focus {
            border-color: var(--color-gold-dark);
            box-shadow: 0 0 0 2px var(--color-gold-light);
            outline: none;
        }
    </style>
</head>
<body class="bg-gray-100">

    <main class="container mx-auto max-w-2xl p-4 sm:p-8 mt-8">
        
        <div class="bg-white shadow-xl rounded-lg overflow-hidden">
            <div class="bg-gray-50 p-5 border-b border-gray-200 flex justify-between items-center">
                <h1 class="text-2xl font-bold text-gray-800"><%= pageTitle %></h1>
                <a href="/admin/servicos" class="text-sm text-gray-600 hover:text-gold-dark transition-colors">
                    <i class="fas fa-arrow-left mr-1"></i> Voltar para Lista
                </a>
            </div>

            <form action="<%= editing ? '/admin/servicos/' + service._id + '/editar' : '/admin/servicos/novo' %>" 
                  method="POST" 
                  class="p-8 space-y-6">

                <% if (error) { %>
                    <div class="mb-6 p-4 bg-red-100 border border-red-400 text-red-700 rounded-lg">
                        <i class="fas fa-exclamation-triangle mr-2"></i><%= error %>
                    </div>
                <% } %>

                <div>
                    <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Nome do Serviço *</label>
                    <input type="text" id="name" name="name" required 
                           value="<%= service.name || '' %>"
                           class="form-input mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:ring-gold-dark focus:border-gold-dark">
                </div>

                <div>
                    <label for="description" class="block text-sm font-medium text-gray-700 mb-1">Descrição</label>
                    <textarea id="description" name="description" rows="3"
                              class="form-textarea mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:ring-gold-dark focus:border-gold-dark"><%= service.description || '' %></textarea>
                    <p class="text-xs text-gray-500 mt-1">Opcional. Aparecerá para o cliente na página do salão.</p>
                </div>

                <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <div>
                        <label for="price" class="block text-sm font-medium text-gray-700 mb-1">Preço (R$) *</label>
                        <input type="number" id="price" name="price" required min="0" step="0.01" 
                               value="<%= service.price || '' %>"
                               placeholder="Ex: 50.00"
                               class="form-input mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:ring-gold-dark focus:border-gold-dark">
                    </div>
                    <div>
                        <label for="duration" class="block text-sm font-medium text-gray-700 mb-1">Duração (minutos) *</label>
                        <input type="number" id="duration" name="duration" required min="1" step="1" 
                               value="<%= service.duration || '' %>"
                               placeholder="Ex: 60"
                               class="form-input mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:ring-gold-dark focus:border-gold-dark">
                    </div>
                </div>

                <div>
                    <label for="imageUrl" class="block text-sm font-medium text-gray-700 mb-1">URL da Imagem (Opcional)</label>
                    <input type="url" id="imageUrl" name="imageUrl" 
                           value="<%= service.imageUrl || '' %>"
                           placeholder="https://exemplo.com/imagem.jpg"
                           class="form-input mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:ring-gold-dark focus:border-gold-dark">
                    <p class="text-xs text-gray-500 mt-1">Link para uma imagem do serviço (se houver).</p>
                </div>

                <div class="flex items-center">
                    <input id="isActive" name="isActive" type="checkbox" 
                           class="form-checkbox h-4 w-4 text-gold-dark border-gray-300 rounded focus:ring-gold-dark"
                           <%= (typeof service.isActive === 'undefined' || service.isActive) ? 'checked' : '' %>>
                    <label for="isActive" class="ml-2 block text-sm text-gray-900">
                        Serviço Ativo (visível para clientes)
                    </label>
                </div>

                <div class="pt-5">
                    <button type="submit"
                            class="w-full bg-gold text-white py-3 px-6 rounded-lg font-semibold hover:opacity-90 transition-opacity shadow flex items-center justify-center">
                        <i class="fas fa-save mr-2"></i>
                        <%= editing ? 'Atualizar Serviço' : 'Salvar Novo Serviço' %>
                    </button>
                </div>

            </form>
        </div>
    </main>

</body>
</html>