<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Redefinir Senha – Studio Kadosh</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
      :root { --color-gold-dark: #B8860B; --color-gold-accent: #DAA520; }
      .text-gold { color: var(--color-gold-dark); }
      .bg-gold { background-color: var(--color-gold-accent); }
      .form-input:focus { box-shadow: 0 0 0 2px var(--color-gold-light); border-color: var(--color-gold-dark); }
      .input-group:focus-within .input-icon { color: var(--color-gold-dark); }
    </style>
</head>
<body class="min-h-screen bg-gray-50 flex items-center justify-center p-4">

    <div class="relative z-10 w-full max-w-md">
        <div class="bg-white rounded-3xl shadow-2xl overflow-hidden">
            
            <div class="bg-gold p-8 text-center">
                 <h1 class="text-3xl font-bold text-white mb-2">
                    Criar Nova Senha
                </h1>
                <p class="text-yellow-100 text-lg"> Studio Kadosh </p>
            </div>

            <div class="p-8">
                
                <% if (error) { %>
                <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-xl">
                    <p class="text-sm text-red-800 font-medium"><%= error %></p>
                </div>
                <% } %>

                <p class="text-gray-600 mb-6">
                  Digite sua nova senha. Ela deve ter no mínimo 6 caracteres.
                </p>

                <form action="/reset/<%= token %>" method="POST" class="space-y-6">
                    
                    <div class="input-group">
                        <label for="password" class="block text-sm font-semibold text-gray-700 mb-2">
                            <i class="fas fa-lock mr-2 text-gold"></i> Nova Senha
                        </label>
                        <div class="relative">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <i class="fas fa-lock input-icon text-gray-400"></i>
                            </div>
                            <input id="password" name="password" type="password" required
                                   autocomplete="new-password"
                                   placeholder="Crie sua nova senha"
                                   class="form-input w-full pl-10 pr-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-gold-dark focus:border-transparent transition-all bg-white">
                        </div>
                    </div>

                    <div class="input-group">
                        <label for="passwordConfirm" class="block text-sm font-semibold text-gray-700 mb-2">
                            <i class="fas fa-check-circle mr-2 text-gold"></i> Confirme a Nova Senha
                        </label>
                        <div class="relative">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <i class="fas fa-lock input-icon text-gray-400"></i>
                            </div>
                            <input id="passwordConfirm" name="passwordConfirm" type="password" required
                                   autocomplete="new-password"
                                   placeholder="Digite a nova senha novamente"
                                   class="form-input w-full pl-10 pr-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-gold-dark focus:border-transparent transition-all bg-white">
                        </div>
                    </div>


                    <div class="pt-4">
                        <button type="submit"
                                class="w-full bg-gold text-white py-4 px-6 rounded-xl font-bold text-lg hover:opacity-90 transition-all flex items-center justify-center">
                            <i class="fas fa-save mr-3"></i>
                            <span>Salvar Nova Senha</span>
                        </button>
                    </div>
                </form>

            </div>
        </div>
    </div>
</body>
</html>