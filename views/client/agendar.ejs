<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Novo Agendamento – <%= orgName %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --color-gold-light: #FFD700;
            --color-gold-dark: #B8860B;
            --color-gold-accent: #DAA520;
        }
        .text-gold { color: var(--color-gold-dark); }
        .bg-gold { background-color: var(--color-gold-accent); }
        .border-gold { border-color: var(--color-gold-light); }
        .ring-gold-dark { --tw-ring-color: var(--color-gold-dark); }
        .input-group:focus-within .input-icon {
            color: var(--color-gold-dark);
        }
        .form-select, .form-input {
            transition: all 0.2s ease-in-out;
        }
         /* Melhora a aparência do input de data */
        input[type="date"]::-webkit-calendar-picker-indicator {
            cursor: pointer;
            opacity: 0.6;
            filter: invert(0.6) sepia(1) saturate(5) hue-rotate(10deg); /* Dourado */
        }
        input[type="date"]:hover::-webkit-calendar-picker-indicator {
            opacity: 0.8;
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen">

    <header class="bg-white shadow-md">
        <nav class="container mx-auto max-w-4xl px-4 py-4 flex justify-between items-center">
            <div>
                <h1 class="text-xl font-bold text-gray-800"><%= orgName %></h1>
                <p class="text-sm text-gray-600">Novo Agendamento</p>
            </div>
            <a href="/portal/minha-area" 
               class="text-sm text-gray-600 hover:text-gold-dark transition-colors"
               title="Voltar para Minha Área">
                <i class="fas fa-arrow-left fa-lg mr-1"></i> Voltar
            </a>
        </nav>
    </header>

    <main class="container mx-auto max-w-4xl p-4 mt-8">

        <div class="bg-white rounded-2xl shadow-lg overflow-hidden">
            <div class="p-5 border-b border-gray-200">
                <h2 class="text-2xl font-bold text-gray-800">Escolha seu Serviço</h2>
            </div>
            
            <div class="p-8">
                <% if (error) { %>
                <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-xl">
                    <p class="text-sm text-red-800 font-medium"><i class="fas fa-exclamation-triangle mr-2"></i><%= error %></p>
                </div>
                <% } %>

                <form action="/portal/agendar" method="POST" class="space-y-6">

                    <div class="input-group">
                        <label for="serviceId" class="block text-sm font-semibold text-gray-700 mb-2">
                            <i class="fas fa-cut mr-2 text-gold"></i> Serviço Desejado
                        </label>
                        <select id="serviceId" name="serviceId" required
                                class="form-select w-full pl-3 pr-10 py-3 border border-gray-300 rounded-xl focus:ring-2 ring-gold-dark focus:border-transparent transition-all bg-white appearance-none">
                            <option value="" disabled selected>-- Selecione um serviço --</option>
                            <% services.forEach(service => { %>
                                <option value="<%= service._id %>" data-duration="<%= service.duration %>">
                                    <%= service.name %> (R$ <%= service.price.toFixed(2).replace('.', ',') %>) - <%= service.duration %> min
                                </option>
                            <% }); %>
                        </select>
                         <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                            <i class="fas fa-chevron-down text-gray-400"></i>
                        </div>
                    </div>

                     <div class="input-group">
                        <label for="staffId" class="block text-sm font-semibold text-gray-700 mb-2">
                            <i class="fas fa-user-tie mr-2 text-gold"></i> Profissional (Opcional)
                        </label>
                        <select id="staffId" name="staffId"
                                class="form-select w-full pl-3 pr-10 py-3 border border-gray-300 rounded-xl focus:ring-2 ring-gold-dark focus:border-transparent transition-all bg-white appearance-none">
                            <option value="">-- Qualquer profissional disponível --</option>
                            <% staff.forEach(member => { %>
                                <option value="<%= member._id %>">
                                    <%= member.name %>
                                </option>
                            <% }); %>
                        </select>
                         <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                            <i class="fas fa-chevron-down text-gray-400"></i>
                        </div>
                         <p class="text-xs text-gray-500 mt-1">Se não escolher, o salão indicará um profissional.</p>
                    </div>

                    <div class="input-group">
                        <label for="date" class="block text-sm font-semibold text-gray-700 mb-2">
                            <i class="fas fa-calendar-alt mr-2 text-gold"></i> Data
                        </label>
                        <input id="date" name="date" type="date" required
                               min="<%= new Date().toISOString().split('T')[0] %>" class="form-input w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 ring-gold-dark focus:border-transparent transition-all bg-white">
                    </div>

                    <div class="input-group">
                        <label for="time" class="block text-sm font-semibold text-gray-700 mb-2">
                            <i class="fas fa-clock mr-2 text-gold"></i> Horário
                        </label>
                        <select id="time" name="time" required
                                class="form-select w-full pl-3 pr-10 py-3 border border-gray-300 rounded-xl focus:ring-2 ring-gold-dark focus:border-transparent transition-all bg-white appearance-none">
                            <option value="" disabled selected>-- Selecione a data e profissional --</option>
                            <% ['08:00', '08:30', '09:00', '09:30', '10:00', '10:30', '11:00', '11:30', '13:00', '13:30', '14:00', '14:30', '15:00', '15:30', '16:00', '16:30', '17:00', '17:30', '18:00'].forEach(slot => { %>
                                <option value="<%= slot %>"><%= slot %></option>
                            <% }); %>
                        </select>
                         <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                            <i class="fas fa-chevron-down text-gray-400"></i>
                        </div>
                    </div>

                    <input type="hidden" id="duration" name="duration">

                    <div class="pt-4">
                        <button type="submit"
                                class="w-full bg-gold text-white py-4 px-6 rounded-xl font-bold text-lg hover:opacity-90 transition-all flex items-center justify-center">
                            <i class="fas fa-calendar-check mr-3"></i>
                            <span>Solicitar Agendamento</span>
                        </button>
                         <p class="text-xs text-gray-500 mt-2 text-center">Seu agendamento ficará pendente até ser confirmado pelo salão.</p>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <footer class="text-center py-8">
        <p class="text-sm text-gray-600">
            <i class="fas fa-crown mr-1.5 text-gold"></i>
            <%= orgName %> © <%= new Date().getFullYear() %>
        </p>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const serviceSelect = document.getElementById('serviceId');
            const durationInput = document.getElementById('duration');

            if (serviceSelect && durationInput) {
                serviceSelect.addEventListener('change', function() {
                    const selectedOption = this.options[this.selectedIndex];
                    const duration = selectedOption.getAttribute('data-duration');
                    durationInput.value = duration || ''; // Preenche o campo oculto
                });
            }

            // Futuramente: Adicionar JS para filtrar profissionais por serviço
            // Futuramente: Adicionar JS para buscar horários disponíveis via API
        });
    </script>
</body>
</html>