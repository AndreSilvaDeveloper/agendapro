<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Novo Agendamento – <%= orgName %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --color-gold-light: #FFD700;
            --color-gold-dark: #B8860B;
            --color-gold-accent: #DAA520;
        }
        .text-gold { color: var(--color-gold-dark); }
        .bg-gold { background-color: var(--color-gold-accent); }
        .border-gold { border-color: var(--color-gold-light); }
        .ring-gold-dark { --tw-ring-color: var(--color-gold-dark); }
        
        /* Foco do ícone dentro do input */
        .input-group:focus-within .input-icon {
            color: var(--color-gold-dark);
        }
        .form-select, .form-input {
            transition: all 0.2s ease-in-out;
        }

         /* Melhora a aparência do input de data */
        input[type="date"]::-webkit-calendar-picker-indicator {
            cursor: pointer;
            opacity: 0.6;
            filter: invert(0.6) sepia(1) saturate(5) hue-rotate(10deg); /* Dourado */
        }
        input[type="date"]:hover::-webkit-calendar-picker-indicator {
            opacity: 0.8;
        }
        select:disabled, input:disabled {
            background-color: #f3f4f6; /* bg-gray-100 */
            opacity: 0.7;
            cursor: not-allowed;
        }

        /* Animação de Fade-in */
        .animate-fade-in {
            animation: fadeIn 0.5s ease-in-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Efeito de Texto Gradiente */
        .text-gradient-gold {
            background: linear-gradient(90deg, #B8860B, #FFD700, #DAA520, #FFD700, #B8860B);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-fill-color: transparent;
            background-size: 200% auto;
            animation: text-shine 4s linear infinite;
        }
        @keyframes text-shine {
            from { background-position: 200% center; }
            to { background-position: 0% center; }
        }

        /* Efeito de Brilho no Background */
        .bg-shine-gold {
            position: relative;
            overflow: hidden;
        }
        .bg-shine-gold::after {
            content: ''; position: absolute; top: -10%; left: -75%;
            width: 50%; height: 120%;
            background: linear-gradient(90deg, rgba(255, 255, 255, 0) 0%, rgba(255, 255, 255, 0.4) 50%, rgba(255, 255, 255, 0) 100%);
            transform: skewX(-25deg);
            animation: bg-shine 5s infinite linear;
        }
        @keyframes bg-shine {
            0% { left: -75%; }
            100% { left: 125%; }
        }

        /* ============================================= */
        /* ===          INÍCIO DA NOVA ALTERAÇÃO         === */
        /* ============================================= */
        body {
          background-color: #f3f4f6; /* Cor bg-gray-100 do Tailwind */
          background-image: url("data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHdpZHRoPSI0IiBoZWlnaHQ9IjQiIHZpZXdCb3g9IjAgMCA0IDQiPjxyZWN0IHdpZHRoPSI0IiBoZWlnaHQ9IjQiIGZpbGw9IiNmM2Y0ZjYiPjwvcmVjdD48cmVjdCB3aWR0aD0iMSIgaGVpZ2h0PSIxIiBmaWxsPSIjZTJlNThlIj48L3JlY3Q+PC9zdmc+");
        }
        /* ============================================= */
        /* ===           FIM DA NOVA ALTERAÇÃO           === */
        /* ============================================= */

    </style>
</head>
<body class="bg-gray-100 min-h-screen font-sans">

    <header class="bg-white shadow-sm border-b border-gray-100">
        <nav class="container mx-auto max-w-2xl px-4 py-4 flex justify-between items-center">
            <div>
                <h1 class="text-xl font-bold text-gradient-gold"><%= orgName %></h1>
                <p class="text-sm text-gray-600">Novo Agendamento</p>
            </div>
            <a href="/portal/minha-area" 
               class="flex items-center space-x-2 text-sm text-gray-600 hover:text-gold-dark transition-colors px-3 py-2 rounded-lg hover:bg-gray-100"
               title="Voltar para Minha Área">
                <i class="fas fa-arrow-left fa-lg"></i>
                <span class="font-medium">Voltar</span>
            </a>
        </nav>
    </header>

    <main class="container mx-auto max-w-2xl p-4 mt-8 mb-8"> {/* Adicionado mb-8 para espaço inferior */}

        <div class="bg-white rounded-2xl shadow-lg overflow-hidden animate-fade-in">
            <div class="p-6 border-b border-gray-200">
                <h2 class="text-2xl font-bold text-gray-800 flex items-center">
                    <i class="fas fa-calendar-plus text-gradient-gold mr-3"></i>
                    Escolha seu Horário
                </h2>
                <p class="text-sm text-gray-600 mt-1">Siga os 4 passos para solicitar seu agendamento.</p>
            </div>
            
            <div class="p-6 sm:p-8">
                <% if (error) { %>
                <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-xl">
                    <p class="text-sm text-red-800 font-medium"><i class="fas fa-exclamation-triangle mr-2"></i><%= error %></p>
                </div>
                <% } %>
                
                <% if (success) { %>
                <div class="mb-6 p-4 bg-green-50 border border-green-200 rounded-xl">
                    <p class="text-sm text-green-800 font-medium"><i class="fas fa-check-circle mr-2"></i><%= success %></p>
                </div>
                <% } %>

                <form action="/portal/agendar" method="POST" class="space-y-6">

                    <div>
                        <label for="serviceId" class="block text-sm font-semibold text-gray-700 mb-1">
                            1. Serviço Desejado
                        </label>
                        <div class="relative input-group">
                            <span class="absolute inset-y-0 left-0 flex items-center pl-3.5 pointer-events-none">
                                <i class="fas fa-cut text-gray-400 input-icon transition-colors"></i>
                            </span>
                            <select id="serviceId" name="serviceId" required
                                    class="form-select w-full pl-10 pr-10 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-gold-dark focus:border-gold-dark transition-all bg-white appearance-none">
                                <option value="" disabled selected>-- Selecione um serviço --</option>
                                <% services.forEach(service => { %>
                                    <option value="<%= service._id %>" data-duration="<%= service.duration %>">
                                        <%= service.name %> (R$ <%= service.price.toFixed(2).replace('.', ',') %>) - <%= service.duration %> min
                                    </option>
                                <% }); %>
                            </select>
                        </div>
                    </div>

                    <div>
                        <label for="staffId" class="block text-sm font-semibold text-gray-700 mb-1">
                            2. Profissional
                        </label>
                        <div class="relative input-group">
                            <span class="absolute inset-y-0 left-0 flex items-center pl-3.5 pointer-events-none">
                                <i class="fas fa-user-tie text-gray-400 input-icon transition-colors"></i>
                            </span>
                            <select id="staffId" name="staffId" required disabled
                                    class="form-select w-full pl-10 pr-10 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-gold-dark focus:border-gold-dark transition-all bg-white appearance-none">
                                <option value="" disabled selected>-- Selecione um serviço primeiro --</option>
                            </select>
                        </div>
                    </div>

                    <div>
                        <label for="date" class="block text-sm font-semibold text-gray-700 mb-1">
                            3. Data
                        </label>
                         <div class="relative input-group">
                            <span class="absolute inset-y-0 left-0 flex items-center pl-3.5 pointer-events-none">
                                <i class="fas fa-calendar-alt text-gray-400 input-icon transition-colors"></i>
                            </span>
                            <input id="date" name="date" type="date" required disabled
                                   min="<%= new Date().toISOString().split('T')[0] %>" 
                                   class="form-input w-full pl-10 px-4 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-gold-dark focus:border-gold-dark transition-all bg-white">
                        </div>
                    </div>

                    <div>
                        <label for="time" class="block text-sm font-semibold text-gray-700 mb-1">
                            4. Horário
                        </label>
                         <div class="relative input-group">
                             <span class="absolute inset-y-0 left-0 flex items-center pl-3.5 pointer-events-none">
                                <i class="fas fa-clock text-gray-400 input-icon transition-colors"></i>
                            </span>
                            <select id="time" name="time" required disabled
                                    class="form-select w-full pl-10 pr-10 py-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-gold-dark focus:border-gold-dark transition-all bg-white appearance-none">
                                <option value="" disabled selected>-- Selecione o profissional e a data --</option>
                            </select>
                        </div>
                    </div>

                    <input type="hidden" id="duration" name="duration">

                    <div class="pt-4">
                        <button type="submit" id="submitButton" disabled
                                class="w-full bg-gold text-white py-4 px-6 rounded-xl font-bold text-lg transition-all flex items-center justify-center disabled:opacity-50 disabled:cursor-not-allowed hover:opacity-90 bg-shine-gold">
                            <i class="fas fa-calendar-check mr-3"></i>
                            <span>Solicitar Agendamento</span>
                        </button>
                         <p class="text-xs text-gray-500 mt-3 text-center">
                            <i class="fas fa-info-circle mr-1"></i>
                            Seu agendamento ficará pendente até ser confirmado pelo salão.
                         </p>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <footer class="text-center py-8 mt-4">
        <div class="container mx-auto max-w-2xl px-4">
            <p class="text-sm text-gray-600">
                <i class="fas fa-crown mr-1.5 text-gold"></i>
                <%= orgName %> © <%= new Date().getFullYear() %>
            </p>
        </div>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Script do formulário dinâmico (sem alterações)
            const serviceSelect = document.getElementById('serviceId');
            const staffSelect = document.getElementById('staffId');
            const dateInput = document.getElementById('date');
            const timeSelect = document.getElementById('time');
            const durationInput = document.getElementById('duration');
            const submitButton = document.getElementById('submitButton');

            serviceSelect.addEventListener('change', handleServiceChange);
            staffSelect.addEventListener('change', handleStaffOrDateChange);
            dateInput.addEventListener('change', handleStaffOrDateChange);
            timeSelect.addEventListener('change', handleTimeChange);

            async function handleServiceChange() {
                const serviceId = serviceSelect.value;
                const selectedOption = serviceSelect.options[serviceSelect.selectedIndex];
                durationInput.value = selectedOption.getAttribute('data-duration') || '60';

                resetSelect(staffSelect, '-- Carregando profissionais... --');
                resetSelect(timeSelect, '-- Selecione profissional e data --');
                dateInput.disabled = true;
                dateInput.value = ''; 

                if (!serviceId) {
                    resetSelect(staffSelect, '-- Selecione um serviço primeiro --');
                    return; 
                }

                try {
                    const response = await fetch(`/api/portal/staff-by-service/${serviceId}`);
                    if (!response.ok) throw new Error('Erro ao buscar profissionais.');
                    
                    const staffList = await response.json();
                    
                    resetSelect(staffSelect, '-- Escolha um profissional --');
                    staffList.forEach(staff => {
                        staffSelect.add(new Option(staff.name, staff._id));
                    });
                    staffSelect.disabled = false;

                } catch (error) {
                    console.error('Erro em handleServiceChange:', error);
                    resetSelect(staffSelect, '-- Erro ao carregar --');
                }
            }

            async function handleStaffOrDateChange() {
                const serviceId = serviceSelect.value;
                const staffId = staffSelect.value;
                const date = dateInput.value;

                if (staffId) {
                    dateInput.disabled = false;
                }

                resetSelect(timeSelect, '-- Selecione profissional e data --');

                if (!serviceId || !staffId || !date) {
                    return;
                }

                resetSelect(timeSelect, '-- Carregando horários... --');
                
                try {
                    const params = new URLSearchParams({ serviceId, staffId, date });
                    const response = await fetch(`/api/portal/available-times?${params}`);
                    
                    if (!response.ok) throw new Error('Erro ao buscar horários.');

                    const availableSlots = await response.json();

                    if (availableSlots.length > 0) {
                        resetSelect(timeSelect, '-- Escolha um horário --');
                        availableSlots.forEach(slot => {
                            timeSelect.add(new Option(slot, slot));
                        });
                        timeSelect.disabled = false; 
                    } else {
                        resetSelect(timeSelect, '-- Nenhum horário disponível --');
                    }
                } catch (error) {
                    console.error('Erro em handleStaffOrDateChange:', error);
                    resetSelect(timeSelect, '-- Erro ao carregar horários --');
                }
            }
            
            function handleTimeChange() {
                if (timeSelect.value) {
                    submitButton.disabled = false;
                } else {
                    submitButton.disabled = true;
                }
            }

            function resetSelect(selectElement, defaultText) {
                selectElement.innerHTML = ''; 
                const defaultOption = new Option(defaultText, '');
                defaultOption.disabled = true;
                defaultOption.selected = true;
                selectElement.add(defaultOption);
                selectElement.disabled = true; 
                submitButton.disabled = true; 
            }
        });
    </script>
</body>
</html>